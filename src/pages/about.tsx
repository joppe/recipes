import * as React from 'react';

import { Layout } from '../components/Layout';

export default function (): JSX.Element {
    return (
        <Layout>
            <div>About what?!</div>
        </Layout>
    );
}
