import { Autocomplete } from '../Autocomplete';

export default {
  title: 'Component/Autocomplete',
  component: Autocomplete,
};

const Template = () => <Autocomplete />;

export const Default = Template.bind({});
