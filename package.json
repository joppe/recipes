{
  "name": "recipes",
  "version": "0.0.1",
  "scripts": {
    "graphql:compile": "graphql-codegen --config codegen.ts",
    "graphql:watch": "graphql-codegen --watch --config codegen.ts",
    "prettier:backend": "prettier --write ./backend/src",
    "prettier:frontend": "prettier --write ./frontend"
  },
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend",
    "packages/config/*",
    "packages/ui"
  ],
  "devDependencies": {
    "@graphql-codegen/cli": "^3.2.2",
    "@graphql-codegen/client-preset": "^2.1.1",
    "@recipes/eslint-config": "*",
    "@recipes/prettier-config": "*",
    "eslint": "^8.38.0"
  }
}
